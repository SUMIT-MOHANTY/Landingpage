# Images Directory

This directory contains all the images used in the application.

## Guidelines for images

1. Use optimized images (compressed JPG/PNG or SVG) for better performance
2. Maintain a consistent naming convention (kebab-case preferred)
3. Use descriptive filenames that indicate the content of the image
4. For responsive design, consider multiple image sizes when appropriate
